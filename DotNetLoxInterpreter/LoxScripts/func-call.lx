fun tryingToGo(a, b, c) {
  print "Hello world";
  print "Something is happening here [" + a + b + c + "]";
}

tryingToGo(1, 2, 3);

// Try to invoke a native function "clock"

print "Now is seconds " + clock();